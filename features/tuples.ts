const drink = {
  color: "brown",
  carbonated: true,
  sugar: 40,
};

type Drink = [string, boolean, number];

const pepsi: Drink = ["brown", true, 40];
const sprite: Drink = ["clear", true, 30];

const carSpec: [number, number] = [400, 500];

const carSpec2 = {
  horsePower: 400,
  weight: 3354,
};
